interface GodricsHat {
  void insertion(int[] array);
  void merge(int[] array);               // use recursion only
  void quick(int[] array, int p, int r); // use recursion only
  void quickLoopy(int[] array);
  void counting(int[] array);
}